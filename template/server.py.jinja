"""{{ project_name }} — skills & prompts MCP server."""

import dropmcp

if __name__ == "__main__":
    dropmcp.run(
        name="{{ project_name }}",
        skills="skills",
        prompts="prompts",
    )
