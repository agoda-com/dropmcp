# Supporting file example

This file lives next to `SKILL.md`, so `dropmcp` exposes it as a resource
link (`skill://hello-world/reference.md`) when the skill tool is invoked.
Use supporting files for longer references, scripts, or templates that the
agent should fetch only when needed.
